# Product Requirements Document: Hospital Patient Tracking Dashboard

## 1. ภาพรวมผลิตภัณฑ์

ระบบ Hospital Patient Tracking Dashboard เป็นเว็บแอปสำหรับติดตามสถานะผู้ป่วยที่เข้ารับบริการในโรงพยาบาลแบบรายวันและย้อนหลัง โดยเน้นให้เจ้าหน้าที่คัดกรอง พยาบาลห้องตรวจ และผู้บริหารสามารถดูจำนวนผู้ป่วย แยกตามสถานะ แผนก สิทธิการรักษา และประเภทผู้ป่วยได้อย่างรวดเร็ว

เป้าหมายหลักคือเปลี่ยนรูปแบบการนับจำนวนและตรวจสถานะจากงานเอกสารหรือไฟล์ Excel แยกส่วน ให้เป็นแดชบอร์ดกลางที่ค้นหา กรองข้อมูล และส่งออกรายงานได้ในจุดเดียว

## 2. วัตถุประสงค์

- ลดเวลาการรวบรวมจำนวนผู้ป่วยประจำวัน
- ช่วยให้แต่ละจุดบริการเห็นสถานะผู้ป่วยแบบใกล้เคียงเวลาจริง
- ให้ผู้บริหารติดตามภาพรวมงานบริการและภาระงานของแต่ละแผนก
- ลดความผิดพลาดจากการคีย์ข้อมูลซ้ำหรือรวมข้อมูลด้วยมือ
- เตรียมข้อมูลให้นำไปวิเคราะห์ต่อหรือส่งออกเป็น CSV/XLSX ได้

## 3. กลุ่มผู้ใช้และสิทธิ์

### 3.1 เจ้าหน้าที่คัดกรอง

หน้าที่หลัก:

- ลงทะเบียนหรือค้นหาข้อมูลผู้ป่วยที่มารับบริการ
- ระบุประเภทผู้ป่วย เช่น OPD, Emergency, Refer
- ระบุแผนกหรือจุดบริการปลายทาง
- เปลี่ยนสถานะเริ่มต้นเป็น `waiting`

สิทธิ์:

- ดูรายการผู้ป่วยที่เกี่ยวข้องกับจุดคัดกรอง
- เพิ่ม visit ใหม่
- แก้ไขข้อมูล visit ที่ยังไม่เสร็จสิ้น
- ไม่สามารถดูรายงานเชิงบริหารทั้งหมดได้

### 3.2 พยาบาลห้องตรวจ

หน้าที่หลัก:

- ดูคิวผู้ป่วยของแผนกตนเอง
- เปลี่ยนสถานะจาก `waiting` เป็น `in_progress`
- ปิดงานเป็น `completed`
- ส่งต่อผู้ป่วยเป็น `referred` เมื่อจำเป็น

สิทธิ์:

- ดูรายการ visit เฉพาะแผนกที่รับผิดชอบ
- อัปเดตสถานะ visit
- ดูสถิติของแผนกตนเอง
- ไม่สามารถแก้ไขข้อมูลตั้งต้นของผู้ป่วยนอกเหนือจากข้อมูล visit ได้

### 3.3 ผู้บริหาร

หน้าที่หลัก:

- ดูภาพรวมจำนวนผู้ป่วยทั้งหมด
- เปรียบเทียบจำนวนผู้ป่วยตามช่วงวันที่ แผนก สิทธิการรักษา และประเภทผู้ป่วย
- ดาวน์โหลดรายงาน CSV/XLSX

สิทธิ์:

- ดูข้อมูลรวมทุกแผนก
- ใช้ตัวกรองทั้งหมด
- ส่งออกรายงาน
- ไม่เน้นการแก้ไขสถานะราย visit ยกเว้นระบบกำหนดให้เป็น admin

## 4. ขอบเขตของระบบ

### 4.1 ฟีเจอร์ที่ต้องมี

- หน้าแดชบอร์ดสรุปจำนวนผู้ป่วย
- การ์ดสถิติ 5 สถานะ:
  - Total
  - Completed
  - Waiting
  - In-Progress
  - Referred
- ตารางรายการผู้ป่วยพร้อม pagination
- ตัวกรองข้อมูลแบบ dynamic
- ค้นหาผู้ป่วยจากชื่อ HN หรือ VN
- เปลี่ยนสถานะ visit
- ส่งออกรายงานเป็น CSV และ XLSX
- แยกสิทธิ์การเข้าถึงตาม role

### 4.2 ฟีเจอร์ที่ยังไม่รวมในระยะแรก

- ระบบนัดหมายล่วงหน้า
- ระบบ billing หรือค่าใช้จ่าย
- การเชื่อมต่อ HIS จริง
- ระบบแจ้งเตือนผ่าน LINE หรือ SMS
- ระบบ audit log แบบละเอียดทุก field

## 5. User Flow หลัก

### 5.1 เจ้าหน้าที่คัดกรองเพิ่มผู้ป่วยเข้าระบบ

1. เจ้าหน้าที่เข้าสู่ระบบ
2. เปิดหน้าคัดกรองหรือหน้า visit list
3. ค้นหาผู้ป่วยจาก HN หรือเพิ่มข้อมูลผู้ป่วยใหม่
4. เลือกแผนกปลายทาง
5. เลือกสิทธิการรักษา
6. เลือกประเภทผู้ป่วย
7. ระบบสร้าง visit พร้อมสถานะ `waiting`
8. รายการผู้ป่วยปรากฏในตารางของแผนกปลายทาง

### 5.2 พยาบาลอัปเดตสถานะผู้ป่วย

1. พยาบาลเข้าสู่ระบบ
2. เปิดหน้ารายการผู้ป่วยของแผนก
3. กรองหรือค้นหาผู้ป่วย
4. กดเริ่มตรวจเพื่อเปลี่ยนสถานะเป็น `in_progress`
5. เมื่อจบการให้บริการ เปลี่ยนสถานะเป็น `completed`
6. หากต้องส่งต่อ เปลี่ยนสถานะเป็น `referred` และระบุหมายเหตุ

### 5.3 ผู้บริหารดูรายงาน

1. ผู้บริหารเข้าสู่ระบบ
2. เปิดหน้าแดชบอร์ด
3. เลือกช่วงวันที่
4. เลือกแผนก สิทธิการรักษา หรือประเภทผู้ป่วย
5. ระบบแสดงการ์ดสรุปและตารางที่อัปเดตตามตัวกรอง
6. ผู้บริหารดาวน์โหลดรายงาน CSV หรือ XLSX

## 6. ตัวกรองข้อมูล

ระบบต้องรองรับตัวกรองต่อไปนี้:

| ตัวกรอง | รายละเอียด | ค่าเริ่มต้น |
| --- | --- | --- |
| Date Range | ช่วงวันที่เข้ารับบริการ | วันนี้ |
| Department | แผนกหรือจุดบริการ | ทุกแผนก |
| Insurance Type | สิทธิการรักษา | ทุกสิทธิ |
| Patient Type | ประเภทผู้ป่วย | ทุกประเภท |
| Status | สถานะ visit | ทุกสถานะ |
| Search | ค้นหาจาก HN, VN, ชื่อ, นามสกุล | ว่าง |

เมื่อผู้ใช้เปลี่ยนตัวกรอง ระบบต้องโหลดข้อมูลสถิติและตารางใหม่โดยใช้ filter เดียวกันทั้งหน้า

## 7. สถานะผู้ป่วย

| Status | ความหมาย | ใช้เมื่อ |
| --- | --- | --- |
| `waiting` | รอรับบริการ | ลงทะเบียนหรือคัดกรองเสร็จแล้ว |
| `in_progress` | กำลังรับบริการ | เจ้าหน้าที่เริ่มดำเนินการ |
| `completed` | เสร็จสิ้น | รับบริการจบแล้ว |
| `referred` | ส่งต่อ | ต้องส่งต่อไปแผนกหรือสถานพยาบาลอื่น |

การ์ด `Total` ไม่ใช่สถานะในฐานข้อมูล แต่เป็นจำนวน visit ทั้งหมดตามตัวกรองปัจจุบัน

## 8. หน้าจอหลัก

### 8.1 Dashboard

องค์ประกอบ:

- Header แสดงชื่อระบบและข้อมูลผู้ใช้
- Filter bar สำหรับเลือกช่วงวันที่ แผนก สิทธิการรักษา ประเภทผู้ป่วย และสถานะ
- Summary cards จำนวน 5 ใบ
- ปุ่ม Export CSV และ Export XLSX ใต้การ์ดสรุป
- ตารางรายการผู้ป่วย
- Pagination ด้านล่างตาราง

### 8.2 ตารางรายการผู้ป่วย

คอลัมน์ที่ต้องมี:

| Column | รายละเอียด |
| --- | --- |
| VN | เลข visit |
| HN | เลขประจำตัวผู้ป่วย |
| Patient Name | ชื่อและนามสกุล |
| Department | แผนก |
| Patient Type | ประเภทผู้ป่วย |
| Insurance | สิทธิการรักษา |
| Status | ป้ายสถานะ |
| Visit Date | วันเวลาเข้ารับบริการ |
| Actions | ปุ่มดูรายละเอียดหรือเปลี่ยนสถานะ |

## 9. Business Rules

- Visit ใหม่ต้องเริ่มต้นด้วยสถานะ `waiting`
- พยาบาลสามารถอัปเดตสถานะได้เฉพาะ visit ในแผนกของตนเอง
- ผู้บริหารสามารถดูข้อมูลทุกแผนก แต่การแก้ไขข้อมูลต้องเป็นไปตามสิทธิ์ admin เท่านั้น
- หากสถานะเป็น `completed` แล้ว ไม่ควรกลับไปเป็น `waiting` ยกเว้นผู้ใช้มีสิทธิ์ admin
- การ export ต้องอ้างอิงตัวกรองเดียวกับข้อมูลที่แสดงบนหน้าจอ
- จำนวนในการ์ดสรุปต้องตรงกับข้อมูลตารางเมื่อใช้ filter เดียวกัน

## 10. Non-Functional Requirements

- หน้าแดชบอร์ดควรโหลดข้อมูลครั้งแรกภายใน 2 วินาที เมื่อข้อมูลไม่เกิน 10,000 visits
- API ตารางต้องรองรับ pagination
- API สถิติต้องคำนวณจากฐานข้อมูล ไม่ hard-code
- UI ต้องรองรับหน้าจอ desktop และ tablet
- ข้อมูลที่เป็นข้อมูลส่วนบุคคลต้องแสดงเท่าที่จำเป็น
- ต้องตรวจสอบสิทธิ์ทุก API endpoint ฝั่ง backend

## 11. Acceptance Criteria

- ผู้ใช้แต่ละ role เห็นข้อมูลตามสิทธิ์ของตนเอง
- เมื่อเปลี่ยน filter แล้ว summary cards และตารางเปลี่ยนตาม
- จำนวน `Total` เท่ากับผลรวม visit ทั้งหมดตามตัวกรอง
- จำนวน `Completed`, `Waiting`, `In-Progress`, `Referred` ถูกต้องตามสถานะ
- ตารางแบ่งหน้าได้ และเปลี่ยนหน้าแล้วข้อมูลถูกต้อง
- ปุ่ม Export CSV/XLSX ดาวน์โหลดไฟล์ได้จริง
- ป้ายสถานะมีสีตรงตามคู่มือ UI ใน `design.md`

